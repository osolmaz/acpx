{
  "name": "acpx",
  "version": "0.1.0",
  "description": "Headless CLI client for the Agent Client Protocol (ACP) — talk to coding agents from the command line",
  "type": "module",
  "bin": {
    "acpx": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup src/cli.ts --format esm --dts --clean",
    "dev": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "acp",
    "agent-client-protocol",
    "cli",
    "codex",
    "claude-code",
    "coding-agent",
    "ai"
  ],
  "author": "Janitr AI",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/janitrai/acpx.git"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^0.14.1",
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
